using UnityEngine;

public class DatosInteresantes : MonoBehaviour
{

    public float x;
    public float y;

    // Start is called before the first frame update
    void Start()
    {
        x = -1.46f;
        y = 0.15f;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
